{
  "name": "kakao-promise",
  "version": "0.1.0",
  "description": "Simple promise wrapper for Kakao sdk with high usability",
  "main": "index.js",
  "repository": "https://github.com/zeakd/kakao-promise",
  "author": "zeakd",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rimraf lib",
    "prebuild": "npm run clean",
    "build": "babel src/ -d lib/",
    "prebuild:watch": "npm run clean",
    "build:watch": "babel src/ -d lib/ --watch",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "dotenv": "^4.0.0",
    "mocha": "^4.0.1",
    "rimraf": "^2.6.2",
    "should": "^13.1.3"
  },
  "files": [
    "lib" 
  ]
}
